Holdout Randomization Tests
==============================

The pyhrt package provides an implementation of holdout randomization tests (HRTs) for assigning p-values to features using predictive models.

